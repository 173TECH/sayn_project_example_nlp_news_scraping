tasks:

  # AutoSQL

  # Remove duplicates generated by load_data task
  dim_reddit_feeds:
    preset: modelling
    parents:
      - load_data


  # Provides some basic statistics on text within links, grouped by link
  dim_reddit_feeds_nlp_stats:
    preset: modelling
    materialisation: view
    parents:
      - nlp
